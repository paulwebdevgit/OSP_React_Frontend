import React from 'react';
import {Box} from "@mui/material";

const TodoListOverviewComponent = () => {
   
    //todo: show Data as a list (name and description)
    return (
        <Box className="todo">
        </Box>
    );
};

export default TodoListOverviewComponent;