import React from 'react';
import {Box} from "@mui/material";


const AddTodo = () => {


    // todo: create handleOnChange function for 2 inputs

    // todo: create a button and send the input data to the server per click (use the mutation function from react-query) and clear the input fields

    return (
        <div>
            <Box>
            </Box>
        </div>
    );
};

export default AddTodo;